apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{ .Values.spec.template.metadata.labels }}
  template:
    metadata:
      labels:
        {{ .Values.spec.template.metadata.labels }}
    spec:
      containers:
        - name: singbox
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - run
            - -c
            - /etc/sing-box/config.json
          volumeMounts:
            - name: config
              mountPath: /etc/sing-box
      volumes:
        - name: config
          configMap:
            name: {{ .Release.Name }}-config

